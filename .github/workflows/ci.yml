name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Install ffmpeg + xvfb (ubuntu)
        # pynput's keyboard backend needs an X connection to import; xvfb gives
        # the headless runner a virtual display so the import (and the
        # pynput-dependent parsing tests) succeed.
        if: runner.os == 'Linux'
        run: sudo apt-get update -qq && sudo apt-get install -y ffmpeg xvfb

      - name: Install ffmpeg (macos)
        if: runner.os == 'macOS'
        run: brew install ffmpeg || true

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install package + dev deps
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run pytest (ubuntu, under xvfb)
        if: runner.os == 'Linux'
        run: xvfb-run -a pytest -ra --tb=short

      - name: Run pytest (macos)
        if: runner.os == 'macOS'
        run: pytest -ra --tb=short
